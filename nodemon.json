{
  "watch": ["server/*"],
  "ext": "ts",
  "exec": "ts-node --project tsconfig.server.json src/server/index.ts",
  "delay": 500,
  "events": {
    "restart": "eslint 'src/server/**/*.{ts,tsx}'",
    "start": "eslint 'src/server/**/*.{ts,tsx}'"
  }
}
