import { Common } from '@sravni/cosago-react-library/lib/constants';

export const MAX_POLLING_PERIOD = Common.MINUTE * 6;
export const ORDER_MAX_POLLING_PERIOD = Common.MINUTE * 5;
export const STANDARD_POLLING_PERIOD = Common.SECOND;
