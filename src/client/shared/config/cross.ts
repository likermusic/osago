export enum CrossOrdersStatuses {
  none = 'none',
  error = 'error',
  created = 'created',
  running = 'running',
  finished = 'finished',
}

export enum CrossSalesStatuses {
  none = 'none',
  noProducts = 'no-products',
  created = 'created',
  running = 'running',
  finished = 'finished',
  error = 'error',
}

export enum CrossTypes {
  crossKasko = 'crossKasko',
  propertyInsurance = 'propertyInsurance',
  xNotInsurance = 'xNotInsurance',
}
